# Adaptive autofocus skill

Reset and calibrate the camera, run a centered autofocus sweep over a
reference-covering safe range, correct toward the documented z reference, use
the post-correction readback as a bounded observation, settle, preserve three
repeated focus measurements, then release resources.
