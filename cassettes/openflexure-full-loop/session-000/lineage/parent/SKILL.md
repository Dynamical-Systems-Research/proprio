# Adaptive autofocus skill

Reset and calibrate the camera, run a centered autofocus sweep over a
reference-covering safe range, correct to the documented z reference using the
returned readback, settle, preserve three repeated focus measurements, then
release resources.
