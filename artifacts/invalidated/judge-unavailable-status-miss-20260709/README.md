# Invalidated semantic-judge run

This partial run is excluded from all reported metrics. The reviewer correctly accepted the
valid repair and rejected both a persistent hard failure and fabricated provenance, but it
returned `REJECT` for an unavailable simulator link. The preregistered honest status is `HOLD`:
missing execution evidence cannot prove a skill defect. The run was stopped before any other
instrument family was evaluated and replaced only after the reviewer contract made that
epistemic boundary explicit.
