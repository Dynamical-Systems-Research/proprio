# Invalidated full-transaction curve segmentation

This partial run computed autofocus prominence over the full transaction, including the final
return to the selected peak. That valid return contaminated the edge baseline and caused a false
reject. Reanalysis of the already captured traces identified the primary forward sweep as the
largest positive stage displacement and cleanly separated valid from truncated sweeps. The
partial run is excluded.
